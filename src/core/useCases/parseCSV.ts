import Repository from "../repositories/repository";

const parseCSV = (repo: Repository) => async (file: string): Promise<any> => {

  //parse csv

  //save into repository

  //return promise
}

export default parseCSV;